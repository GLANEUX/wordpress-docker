# Projet d'installation wordpress -> Docker

## Installer le projet :

- .env à créer et remplir
- commandes à executer (à la racine du projet):
```
$ docker-compose up

$ npm -g install @wordpress/env

$ wp-env start
```

